<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/head %>
    <title>Sales Mobile 1.0</title>
    <link rel="stylesheet" href="/css/fontawesome-all.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.3.1.js"></script>
</head>
<style>
    body {
        background-color: #EFEFFB;
    }
</style>

<body style="overflow: hidden;">
    <header>
        <% include ../partials/header %>
    </header>

    <div class="container-fluid formulario">
        <div class="form-group row">
            <div class="col-md-4">
            </div>
            <h2 style="border-bottom: 1px #c5c5c5 solid;">Movimento de Entrada no Estoque</h2>
        </div>

        <div class="form-group row">
            <div class="col-md-4">
            </div>
            <h3 style="border-bottom: 1px #c5c5c5 solid;">
                Data do Movimento:
                <%= movestoque[0].movest_dataent%>
            </h3>
        </div>

        <div class="form-group row">

            <div class="col-md-2">
                <label for="datamov">Data Movimento</label>
                <input type="date" class="form-control" id="datamov" name="movest_dataent" readonly value="<%=moment(movestoque[0].movest_dataent).format('YYYY-DD-MM')%>">
            </div>

            <div class="col-md-1">
                <label for="pedido">Pedido</label>
                <input type="number" maxlength="200" class="form-control" id="pedido" name="movest_pedido" placeholder="Nº"
                    readonly value="<%= movestoque[0].movest_pedido%>">
            </div>

            <div class="col-md-4">
                <label for="prod_codigo">Produto</label>
                <select class="form-control custom-select" name="prod_codigo" id="prod_codigo">
                    <% for (prod of produto){%>
                    <option value="<%= prod.prod_codigo%>" id="codigo-produto-<%= prod.prod_codigo%>">
                        <%= prod.prod_nome%>
                    </option>
                    <%}%>
                </select>
            </div>

            <div class="col-md-1">
                <label for="Qtde">Qtde</label>
                <input type="number" class="form-control" id="qtde" name="qtde" placeholder="000">
            </div>


            <div class="col-md-1">
                <label for="Custo">Custo</label>
                <input type="money" class="form-control" id="item_custo" name="item_custo" placeholder="R$0.00">
            </div>

            <div class="col-md-1">
                <label for="prod_codigo">.</label>
                <button type="button" class="btn btn-primary btn-block" onclick="incluirItemEstoque(<%=movestoque[0].movest_codigo%>)">Incluir</button>
            </div>

            <div class="col-md-1">
                <label color="blue">.</label>
                <a class="btn btn-warning btn-block" onclick="imprimir()">Imprimir</a>
            </div>

            <div class="col-md-9">
            </div>

        </div>

        <div class="container-fluid formulario">
            <div class="form-group row">
                <div class="col-md-5">
                </div>

            </div>
        </div>

        <div style="overflow: auto " class="container-fluid">
            <table class="table table-striped table-ligth table-bordered">
                <thead>
                    <tr>
                        <th>Produto</th>
                        <th>Quantidade</th>
                        <th>Custo</th>
                        <th>Deleta</th>
                    </tr>
                </thead>
                <tbody id="table-body-itens">
                    <% for (item of movestoque){%>
                    <tr id="list-itens">
                        <td>
                            <%= item.prod_nome%>
                        </td>
                        <td>
                            <%= item.qtde%>
                        </td>
                        <td>
                            <%= item.item_custo%>
                        </td>

                        <td><a class="btn btn-danger" href=<%="/itemEstoque/excluir/" + item.item_codigo%>>Deletar</a>
                        </td>
                    </tr>
                    <%}%>
                </tbody>
                </thead>
            </table>
        </div>       
   
        <script src="/js/alterarEstoqueEntrada.js"></script>
</body>

</html>